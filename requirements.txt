# Web Framework
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# ML & Computer Vision
# NOTE: original pins (ultralytics==8.1.28, torch==2.2.0, torchvision==0.17.0,
# numpy==1.26.4) do not ship wheels for Python 3.13, which is what's
# installed on this machine. Bumped to the oldest versions with 3.13 wheels.
# If you run this on Python 3.11 you can pin back to the original versions.
ultralytics>=8.3.0
torch==2.6.0
torchvision==0.21.0
opencv-python-headless==4.10.0.84
Pillow>=10.4.0
numpy>=1.26.4
scipy==1.14.1

# Audio Processing
librosa==0.10.1
soundfile==0.12.1
pydub==0.25.1
# librosa imports pkg_resources internally; setuptools>=81 dropped it,
# breaking audio analysis with "No module named 'pkg_resources'".
setuptools<81

# Video Processing (FFmpeg Python wrapper)
ffmpeg-python==0.2.0

# Database
sqlalchemy>=2.0.36
alembic==1.13.1
psycopg2-binary>=2.9.10
asyncpg>=0.30.0

# AWS S3
boto3==1.34.34
botocore==1.34.34

# Firebase (FCM notifications)
firebase-admin==6.4.0

# Redis Queue
redis==5.0.1
rq==1.16.1
celery==5.3.6

# HTTP Client
httpx==0.26.0
aiohttp>=3.10.11
requests==2.31.0

# Configuration
python-dotenv==1.0.1
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Logging & Monitoring
loguru==0.7.2
sentry-sdk==1.40.6

# Testing
pytest==8.0.1
pytest-asyncio==0.23.5

# Utilities
python-jose==3.3.0
passlib==1.7.4
aiofiles==23.2.1
tqdm==4.66.2
pandas>=2.2.3
